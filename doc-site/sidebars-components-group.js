// @ts-check

// Shared sidebar for the first-party component package doc instances. Every
// component page lists the components on the left (in dependency order: mem
// depends on noc), followed by that package's own pages.
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  componentsGroupSidebar: [
    {
      type: 'category',
      label: 'First-party components',
      collapsed: false,
      items: [
        {type: 'link', label: 'noc', href: '/packages/noc/'},
        {type: 'link', label: 'mem', href: '/packages/mem/'},
      ],
    },
    {type: 'autogenerated', dirName: '.'},
  ],
};

export default sidebars;

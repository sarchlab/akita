{
    "name": "Debug Example() in example_test.go",
    "type": "go",
    "request": "launch",
    "mode": "test",
    "program": "${workspaceFolder}",  
    "args": [
      "-test.run", "^Example$"
    ],
    // optional: set cwd if your code is in a subfolder
    // "cwd": "${workspaceFolder}/path/to/your/package",
    "env": {},
    "buildFlags": "",
    "configurations": [
    
    ]
  }
  
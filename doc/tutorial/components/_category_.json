{
  "label": "Create a Component",
  "position": 3,
  "link": {
    "type": "generated-index",
    "description": "Components are the default Akita pattern. A component bundles immutable configuration (Spec), mutable runtime state (State), and per-cycle behaviour (middleware) into a reusable, named unit. This section builds the single-component random-walk example from scratch, a few lines at a time, and introduces the type Comp = modeling.Component[Spec, State, modeling.None] alias used throughout the codebase."
  }
}
